management:
  endpoints:
    web:
      base-path: /
      exposure:
        include: "*"

server:
  port: 8090
  servlet:
    context-path: /inventario

spring:
  neo4j:
    uri: bolt://0.0.0.0:7687
    database: neo4j
    authentication:
      username: neo4j
      password: rootroot



